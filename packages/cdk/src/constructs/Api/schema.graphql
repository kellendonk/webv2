type InteractionCount {
  id: ID!
  subject: String!
  interaction: String!
  count: Int!
}

type Query {
  getInteractions(subject: String!): [InteractionCount!]!
}

type Mutation {
  addInteraction(subject: String!, interaction: String!): InteractionCount!
}

schema {
  query: Query
  mutation: Mutation
}
