query Query {
  getInteractions(subject: "/josh") {
    id
    interaction
    count
  }
}

mutation Mutation {
  addInteraction(subject: "foo", interaction: "❤️") {
    subject
    interaction
  }
}
