query Query {
  getInteractions(subject: "foo") {
    id
    interaction
    count
  }
}

query Query2 {
  authInfo {
    authority
    clientId
  }
}

mutation Mutation {
  addInteraction(subject: "foo", interaction: "❤️") {
    subject
    interaction
  }
}
